### COMMON SHELL CONFIG ###

aliases:
  g: git
  cm: chezmoi
  vi: "command -v nvim >/dev/null && nvim || command -v vim >/dev/null && vim || command vi"
  fetch: neofetch --config ~/.config/neofetch/simple.conf

env:
  EDITOR: nvim
  PAGER: less -R
